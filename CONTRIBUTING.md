# コントリビューションガイド

このプロジェクトへのコントリビューションを歓迎します。

## はじめに

- 開発環境のセットアップは [docs/getting-started.md](docs/getting-started.md) を参照してください
- コントリビューションには [CLA](CLA.md) への同意が必要です
- 脆弱性の報告は [SECURITY.md](SECURITY.md) の手順に従ってください（Issueには書かないでください）

## PRの出し方

1. リポジトリを fork し、`main` からブランチを切ります
2. 変更を加え、ローカルで `pnpm verify`（test / typecheck / lint / knip / depcruise）を通します
3. `main` に向けてPRを作成します。PRの本文には「なぜこの変更が必要か」を書いてください
4. CIが green になったら、メンテナがレビューしてマージします

外部からのPRでは、CIの実行にメンテナの承認が必要です（GitHub Actions の仕様）。
承認までしばらく時間がかかることがあります。

## AIエージェントによる自動実装について

このリポジトリでは、メンテナが起票した一部のIssueを **AIエージェント（Claude Code）が自律的に実装し、
CIが green になったら自動マージする** 運用を行っています。
仕組みと運用ルールは [docs/loop-engineering.md](docs/loop-engineering.md) に公開しています。

外部コントリビュータの方に関係するのは次の3点です。

- **みなさんのPRが自動でマージされることはありません。** 従来どおりメンテナがレビューしてマージします
- **`loop:` で始まるラベルの付いたIssueはメンテナ管理下のタスク**です。AIが着手中のものもあるため、
  作業が重複しないよう、着手前にIssueにコメントをお願いします。
  外部の方に取り組んでいただきたいタスクには `good first issue` / `help wanted` を付けています
- **`loop:ready` ラベルは外部の方が起票したIssueからは自動で外れます。**
  これは提案を軽視しているのではなく、内容をメンテナが確認してから自動実装の対象にするための仕組みです。
  Issue自体は通常どおり読ませていただきます

## Issue

バグ報告・機能提案はお気軽にどうぞ。再現手順や期待する挙動を書いていただけると助かります。
